/**
 * {{MODULE_NAME}} Module Types
 *
 * Define module-specific types here.
 * For types used across modules, use @/types/ instead.
 */

// Example interface with I prefix
export interface I{{MODULE_NAME_PASCAL}}Data {
  id: string;
  // TODO: Add fields
}

// Example type alias
export type T{{MODULE_NAME_PASCAL}}Status = "active" | "inactive" | "pending";
