{
  "domain": "github",
  "requires": ["github-pr"],
  "commands": ["code_review"],
  "agents": ["reviewer"],
  "setup": {
    "env": [],
    "instructions": "Requires GitHub CLI (gh). Install: brew install gh && gh auth login"
  }
}
