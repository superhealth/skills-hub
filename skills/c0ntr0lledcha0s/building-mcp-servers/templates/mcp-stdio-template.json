{
  "mcpServers": {
    "server-name": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/server.js"],
      "env": {
        "API_KEY": "${YOUR_API_KEY}"
      }
    }
  }
}
