# DB Craft Example Prompts

## Basic Modeling

- Open DB Craft and create a MySQL 8 model for a student management system.
- Use DB Craft to add a table named `student` with fields for name, age, and class_id.
- Open DB Craft and help me turn this screenshot into tables.

## Incremental Updates

- Open DB Craft and continue adding tables to the current workspace model.
- In DB Craft, add an `order` table and relate it to `customer`.
- Update the current DB Craft model by adding indexes for the login and tenant fields.

## SQL Import / Export

- Open DB Craft and import this `CREATE TABLE` SQL into the current model.
- Use DB Craft to validate the current model and export SQL.
- Open DB Craft, review the schema design, and save the exported SQL into the workspace.

## AI-Assisted Use

- Open DB Craft and generate tables for an e-commerce cart, order, and payment flow.
- Use DB Craft AI to create a simple CRM schema with customer, contact, and follow_up tables.
- In DB Craft, generate a PostgreSQL 14 schema for a booking system.

## Review And Handoff

- Open DB Craft, organize the current model, and prepare SQL for handoff.
- Use DB Craft to finish the schema design, then generate artifacts for migration review.
- Open DB Craft and prepare the model plus SQL so another workflow can deploy it later.

## Good Marketplace Examples

- “Design a user, role, and permission schema in DB Craft.”
- “Open DB Craft and convert these requirements into a relational model.”
- “Use DB Craft to build the tables first, then export SQL for implementation.”
